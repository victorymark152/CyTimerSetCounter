name: cytimersetcounter
description: "A new Flutter project for gym countdown timer."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

# ส่วนที่ใช้ตั้งค่า Icon (อยู่ชิดซ้ายสุด)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"

# ส่วนการตั้งค่าของ Flutter (อยู่ชิดซ้ายสุด)
flutter:
  uses-material-design: true

  assets:
    - assets/images/app_icon.png